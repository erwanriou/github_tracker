import { createUseStyles } from "react-jss"

export const useStyles = createUseStyles({
  footerContainer: {
    position: "relative",
    display: "flex",
    justifyContent: "flex-end",
    alignItems: "center",
    width: "100%"
  }
})
